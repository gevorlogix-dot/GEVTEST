[pytest]
addopts = -v --tb=short --html=report.html --self-contained-html
testpaths = tests
python_files = test_*.py

markers =
    smoke: core happy-path tests (login, create report, pay)
    slow: tests that hit the live API and may take time
